<div class="row">
  <div class="col-sm-10 col-sm-offset-1 col-sm-md-10 col-md-offset-1">
  <% @users.each do |user| %>
      <% if user.attachment.present? %><!-- もしユーザーがPDFファイルを編集済みの場合、プロフィールが表示される -->
        <div class ="col-sm-12 col-md-12">
          <div class=""><a href="<%=user_path(user)%>" role="button"><%= user.thesisName %>/(<small><%=user.username %></a></div>
          <div><i class="fa fa-thumbs-o-up"></i> <%= user.get_upvotes.size %>B!</div>
        </div>
        <div class="col-sm-4 col-md-4" >
          <div><%=user.graduate %></div>
          <div><%= user.major %></div>
          <div><!--ここにタグが入る予定です --></div>
        </div>
        <div class="col-sm-8 col-md-8">
          <div><%= user.abstract %></div>
        </div>
    </div>
      <% end %>
  <% end %>
